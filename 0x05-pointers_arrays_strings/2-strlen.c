#include "main.h"
#include <string.h>

/**
 * _strlen - function
 *
 * @s: char for string
 *
 * Return: 0 when successful and 1 for failed
 */

int _strlen(char *s)
{
	int leng = strlen(s);

	return (leng);
}
